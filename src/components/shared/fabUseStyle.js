import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
    fabStyle: {
        position: "fixed",
        bottom: 20,
        right: 20,
    },
    containerStyle: {
        padding: "1.5rem",
    },
});

export default useStyles;
